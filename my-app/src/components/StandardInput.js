import {TextInput } from "react-native";
import React from "react";

const StandardInput = (props) => {
  return <TextInput className="border border-green-100 p-1" {...props} />;
};

export default StandardInput;
